{
  "name": "authart",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend/vite-project",
    "build": "npm run build --prefix frontend/vite-project && node copy-build.js",
    "start": "npm run start --prefix backend"
  }
}
